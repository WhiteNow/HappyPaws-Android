<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="pe.edu.upc.happypaws">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">


        <activity android:name=".controllers.activities.PromoActivity"
                  android:theme="@style/Base.Theme.AppCompat.Light.DarkActionBar"/>

        <activity android:name=".controllers.activities.NewPetActivity"
                  android:label = "Nueva Mascota">
        </activity>
        <activity
                android:name=".controllers.activities.PetProfileScrollingActivity"
                android:label="@string/title_activity_pet_profile_scrolling"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".controllers.activities.PetProfileActivity">
        </activity>
        <activity
                android:name=".controllers.activities.SignUpActivity"
                android:label="@string/title_activity_sign_up"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".controllers.activities.Main2Activity">
        </activity>
        <activity android:name=".controllers.activities.EditProfileActivity">
        </activity>

        <meta-data
                android:name="com.google.android.actions"/>


        <activity android:name=".controllers.activities.NewPetActivity"
                  android:label = "Nueva Mascota">

        </activity>
        <activity
                android:name=".controllers.activities.MainActivity"
                android:label="@string/title_activity_main"
                android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        </activity>
        <activity android:name=".controllers.activities.LoginActivity"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
                android:name="preloaded_fonts"
                android:resource="@array/preloaded_fonts"/>
    </application>

</manifest>